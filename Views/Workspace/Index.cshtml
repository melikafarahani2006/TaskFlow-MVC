@model IEnumerable<TaskFlowMvc.Models.Workspace>

@{
    ViewData["Title"] = "Workspaces";
}

<div class="container py-4">

    <div class="d-flex justify-content-between align-items-center mb-4">

        <div>
            <h2 class="fw-bold mb-1">Workspaces</h2>
            <p class="text-muted">
                Organize your teams, projects and tasks.
            </p>
        </div>

        <a asp-action="Create"
           class="btn btn-primary">
            + New Workspace
        </a>

    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">
            @TempData["Success"]
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">
            @TempData["Error"]
        </div>
    }

    <div class="row g-4">

        @foreach (var item in Model)
        {
            <div class="col-md-6 col-xl-4">

                <div class="card shadow-sm border-0 workspace-card h-100">

                    <div class="card-body">

                        <div class="d-flex align-items-center mb-3">

                            <div class="workspace-icon me-3">
                                🏢
                            </div>

                            <div>
                                <h4 class="mb-0 fw-bold">
                                    @item.Name
                                </h4>
                            </div>

                        </div>

                        <p class="text-muted">

                            @(string.IsNullOrWhiteSpace(item.Description)
                                ? "No description"
                                : item.Description)

                        </p>

                        <hr />


                        <div class="d-flex justify-content-between">

                            <small class="text-muted">
                                Created
                            </small>

                            <small class="fw-semibold">
                                @item.CreatedAt.ToString("yyyy/MM/dd")
                            </small>

                            <small class="text-muted">
                                Updated
                            </small>

                            <small class="fw-semibold">
                                @(item.UpdatedAt.HasValue ? item.UpdatedAt.Value.ToString("dd MMM yyyy") : "-")
                            </small>

                        </div>



                    </div>

                    <div class="card-footer bg-white border-0">

                        <div class="d-flex justify-content-between">

                            <a asp-controller="Project"
                               asp-action="Index"
                               asp-route-workspaceId="@item.Id"
                               class="btn btn-outline-primary btn-sm">

                                Open →

                            </a>

                            <div>

                                <a asp-action="Edit"
                                   asp-route-id="@item.Id"
                                   class="btn btn-warning btn-sm">

                                    Edit

                                </a>

                                <a asp-action="Delete"
                                   asp-route-id="@item.Id"
                                   class="btn btn-danger btn-sm">

                                    Delete

                                </a>

                            </div>

                        </div>

                    </div>

                </div>

            </div>
        }

    </div>

</div>

<style>

    .workspace-card {
        border-radius: 18px;
        transition: .25s;
    }

        .workspace-card:hover {
            transform: translateY(-6px);
            box-shadow: 0 14px 30px rgba(0,0,0,.12) !important;
        }

    .workspace-icon {
        width: 60px;
        height: 60px;
        display: flex;
        justify-content: center;
        align-items: center;
        background: #EEF4FF;
        border-radius: 15px;
        font-size: 28px;
    }

</style>